@model Tiny_Social_Media.Models.Post

<div class="card mb-3 shadow-sm">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-start mb-2">
            <div>
                <span class="badge bg-light text-dark border">
                    <i class="fa-regular fa-user me-1"></i> @Model.User?.Email
                </span>
                <small class="text-muted ms-2">@Model.CreatedAt.ToString("g")</small>
            </div>

            <div class="dropdown">
                <button class="btn btn-link text-muted p-0" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fa-solid fa-ellipsis-vertical"></i>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    @* @if (User.Identity.IsAuthenticated && (User.IsInRole("Admin") || User.Identity.Name == Model.User?.Email))
                    {
                        <li>
                            <form asp-controller="Posts" asp-action="Delete" asp-route-id="@Model.Id" method="post">
                                <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Delete this post?')">
                                    <i class="fa-solid fa-trash-can me-2"></i>Delete Post
                                </button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <li><a class="dropdown-item" href="#"><i class="fa-solid fa-flag me-2"></i>Report</a></li>
                    } *@
                </ul>
            </div>
        </div>

        <p class="card-text">@Model.Content</p>
    </div>
</div>